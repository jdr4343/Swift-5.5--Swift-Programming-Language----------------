//: [Previous](@previous)

import Foundation

//The self Property (‘self’ 속성)
//타입의 모든 인스턴스는, 인스턴스 자체와 정확하게 같은 self 라는 ‘암시적인 (implicit) 속성’ 을 가집니다.5 self 속성은 현재 인스턴스를 자신의 인스턴스 메소드에서 참조하기 위해 사용합니다.

//: [Next](@next)
